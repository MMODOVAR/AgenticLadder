# Core (stdlib-only — no runtime deps required)
# Server
fastapi>=0.110
uvicorn[standard]>=0.29

# Cloud credential providers (all optional)
boto3>=1.34                         # AWS STS
azure-keyvault-secrets>=4.7         # Azure Key Vault
azure-identity>=1.15
google-cloud-secret-manager>=2.20   # GCP Secret Manager
hvac>=2.1                           # HashiCorp Vault
kubernetes>=29                      # Kubernetes ServiceAccount tokens

# Dev / test
pytest>=8
pytest-cov>=5
httpx>=0.27
